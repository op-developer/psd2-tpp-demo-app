doctype html
html(lang='')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title Redirecting...
    script.
        // This page redirects control back to the TPP with parameters in the URL
        // See https://openid.net/specs/openid-connect-core-1_0.html#code-id_tokenExample
        var parameters = location.hash.substring(1);
        var url = 'https://' + window.location.host + window.location.pathname + '?' + parameters
        window.location = url;
  body